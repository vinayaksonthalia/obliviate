# ─────────────────────────────────────────────────────────────
# Obliviate — environment config (copy to .env, fill in values)
# ─────────────────────────────────────────────────────────────

# ── CockroachDB Basic (free) ──
# From Cockroach Cloud → cluster → Connect → General connection string
DATABASE_URL=postgresql://USER:PASSWORD@HOST:26257/defaultdb?sslmode=verify-full

# ── CockroachDB Managed MCP Server ──
COCKROACH_MCP_URL=https://cockroachlabs.cloud/mcp
COCKROACH_MCP_API_KEY=

# ── LLM (LiteLLM-style; Ollama local for dev, hosted for deploy) ──
LLM_PROVIDER=ollama
LLM_MODEL=gemma4:12b
LLM_API_KEY=
LLM_ENDPOINT=
# For deploy, swap to a hosted provider you already have a key for
# (groq / cerebras / openrouter / gemini) via LLM_PROVIDER + LLM_API_KEY.

# ── Embeddings (fastembed, local, 384-d) ──
EMBED_MODEL=BAAI/bge-small-en-v1.5

# ── AWS (deploy: S3 object-locked certificates + Lambda signing) ──
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=ap-south-1
S3_CERT_BUCKET=obliviate-erasure-certs

# ── App secrets (generated locally; never commit real values) ──
OBLIVIATE_ROOT_KEY=      # AES-256 root key for per-subject envelope encryption
APP_SECRET=              # session / auth secret
